import React, { FC } from 'react'

// import hasAccess from '../components/Protected'

const DashboardPage: FC = () => {
    // if (!hasAccess({ to: '/dashboard' })) return null;

    return (
        <div>123</div>
    )
}

export default DashboardPage;